# Printer Hardware (confirmed via lsusb on Pi — Jolimark thermal printer)
PRINTER_VENDOR_ID=0x1ba0
PRINTER_PRODUCT_ID=0x220a

# Service Configuration
PORT=9000

# Paths (used in Docker)
DATA_DIR=/app/data

# API Authentication (optional)
# Set to require X-API-Key header on all print and admin endpoints.
# Leave unset to allow unauthenticated access — suitable for trusted LAN use.
# API_KEY=your-secret-key-here

# Telegram Bot (optional)
# Create a bot via @BotFather on Telegram and paste the token here.
# When set, the bot polls for messages and prints on "print: <text>" or "print todo: Title | Item1 | Item2"
# Leave unset to disable the bot entirely.
# TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxYZ

# GitHub username — used for GHCR image pull in docker-compose.yml
# Set this to your GitHub username (e.g. conorloveis) before running docker compose on the Pi
GITHUB_USER=your-github-username
